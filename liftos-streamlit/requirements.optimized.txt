# Optimized Streamlit requirements with version pinning
# Core framework
streamlit==1.28.1
pandas==2.0.3
numpy==1.24.3

# Visualization (optimized versions)
plotly==5.17.0
altair==5.1.2
matplotlib==3.7.2
seaborn==0.12.2

# HTTP and networking
requests==2.31.0
httpx==0.25.0
aiohttp==3.8.6

# Data processing
scipy==1.11.3
scikit-learn==1.3.0

# Utilities
python-dotenv==1.0.0
pydantic==2.4.2
pillow==10.0.1
uuid

# Streamlit extensions (lightweight)
streamlit-lottie==0.0.5
streamlit-option-menu==0.3.6
streamlit-extras==0.3.5

# Security
cryptography==41.0.7

# Performance optimizations
cachetools==5.3.2
pyarrow==13.0.0  # Fast columnar data format
orjson==3.9.9    # Fast JSON library

# Optional: Remove if not needed
# networkx==3.1  # Only if needed for graph operations